# Alpha Dev 환경 전용 설정

app:
  message: "Hello from Alpha Environment Config Server! (DEV)"
  version: "alpha 1.0.0-dev"
  feature:
    enabled: true
    max-retry: 5
    timeout-seconds: 60

database:
  pool:
    min-size: 2
    max-size: 10
    connection-timeout: 3000

logging:
  level: DEBUG
