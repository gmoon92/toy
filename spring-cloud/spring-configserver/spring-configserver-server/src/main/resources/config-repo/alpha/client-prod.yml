# Alpha Production 환경 전용 설정

app:
  message: "Hello from Alpha Environment Config Server! (PROD)"
  version: "alpha 1.0.0"
  feature:
    enabled: false
    max-retry: 3
    timeout-seconds: 30

database:
  pool:
    min-size: 10
    max-size: 50
    connection-timeout: 10000

logging:
  level: INFO
