group = "com.gmoon.spring-event.spring-kafka.spring-kafka-broker"
description = "spring-kafka-broker"

dependencies {
    implementation project(":java-core")

    implementation platform(libs.testcontainers.bom)
    implementation("org.springframework.boot:spring-boot-starter-web")

    // test
    testImplementation(libs.apache.commons.compress)
    testImplementation(libs.apache.commons.io)
    testImplementation(libs.testcontainers.bom)
    testImplementation(libs.testcontainers.junit5)
    testImplementation(libs.testcontainers.kafka)
}
